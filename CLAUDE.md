# 给 agent 的项目规则

这个仓库几乎全部由 agent 会话写成，同一时间可能有多个会话在改。下面每一条都是
**真实事故换来的**，不是通用建议——照做。

## 一、动手之前：同步

**开工第一件事是 `git fetch origin main`，并确认你的基点不落后。**

```bash
git fetch origin main && git rev-list --count HEAD..origin/main   # 必须是 0
```

worktree 是某个时刻的快照，会静默变陈旧。2026-09-01 有一次：worktree 落后 main
26 个提交，agent 在旧版 README 上做了全文件重写，差点把另一个会话刚加的架构图
连同 26 行内容一起抹掉——CI 不会报错，diff 看起来也正常，因为冲突是**语义上的**。

**重写或重构整个文件之前，先看这个文件最近被谁动过：**

```bash
git log --oneline -3 origin/main -- <文件路径>
```

## 二、别直推 main

agent 一律走分支 + PR。你和用户共用同一套凭据，分支保护规则区分不了，所以这条
靠自觉。PR 的价值是 diff 可审、CI 会跑。

大改动拆成两个提交：**纯搬移一个、内容改动一个**。搬移那个要能逐行验证无损
（写个脚本比对，别靠眼睛）。否则 400 行的删除 diff 没人能审。

## 三、直播转写进行中，禁跑任何模型批量生成

2026-08-31 的事故：批量生成叠在直播 ASR 上，多个模型同时驻留显存，识别从 1.1 秒
掉到 38-55 秒/段，**丢了 16 段音频**——对一个合规监听工具，丢音频就是漏报警。

引擎批测一次只留一个模型，跑前跑后强制 `keep_alive: 0` 卸载。生成脚本自己带硬闸：

```python
for pat in ("tiktokcdn", "main.py --browser"):
    if subprocess.run(["pgrep", "-f", pat], capture_output=True).returncode == 0:
        raise SystemExit("直播转写仍在进行，拒绝批量生成")
```

## 四、引擎对比：不做配对检验就别下结论

2026-08-31 有一次只看百分比差就宣布「12B 碾压 7B、建议换装」，补做 McNemar 后
发现整体 p=0.14 本就不显著，唯一显著的切片是 5 个里挑出来的、经不起多重比较校正，
而且在新语料上还反了方向。**这个错误的建议差点让产品换掉一个更好的引擎。**

- 同源语料用 **McNemar 配对检验**，不比原始百分比
- 切片级发现要做**多重比较校正**（12 组两两检验时阈值是 0.05/12≈0.004）
- `p>0.05` 是「没测出差异」，**不是「等价」**——要主张等价必须看置信区间
- 跨轮的绝对数字不可比：同样 259 条换一批评委重评，major 率会从 9.3% 漂到 12.0%，
  两批评委只有约六成一致。**只信轮内配对。**
- 词表钉写会被评委当成引擎捏造，比引擎前要逐条人工剔除，**别用子串匹配自动豁免**

## 五、违禁词表：只发现，不修改

`banned_terms.txt` 决定什么会触发合规报警，**改它是业务判断，不是 agent 的判断**。
离线审计可以产出候选词条，但必须交人工确认后才入表。词表里那节「待业务确认」的
注释词条同理——不要擅自打开。

新词入表前跑碰撞审计，改完跑回放 gate 比对历史误报：

```bash
python3 tools/collision_audit.py --term <词>
python3 tools/replay_alerts.py
```

## 六、这个产品是什么

**西语带货直播的合规监听器**，服务中控，KPI 只有两个：**违禁词召回**（漏报的代价
远高于误报）和**检测延迟**。由此：

- 翻译必须在关键路径之外：违禁词检测跑在 ASR 的 `raw_text` 上，不等译文
- 音频绝不静默丢弃：宁可晚报警，不可不报警
- **翻译质量再怎么优化，对报警召回的影响都是零**——别把精力错投在这里

## 七、别信「看起来没问题」

- 用 `grep -E "passed|failed"` 判断测试结果会在 "1 failed" 上误判成功。看退出码
- 批量重放 `BannedTermDetector` 必须传真实 `scan(text, ts=...)`，否则 12 秒窗口
  永不过期、退化成 O(n²)（生产无此问题，段间隔 9 秒）
- 会话收尾要认自己的 audit（`my_audit` 模式）：晚死的旧任务关掉新会话的 audit，
  会让合规证据静默丢失，文件只剩一个头

## 八、拿不到流地址：只报观察，不猜原因

2026-09-05/06 一天之内，同一个接口拒绝码 4003110 被先后解释成「年龄限制」
「IP 限流」「被我们的探测打坏」，三个全错，用户差点为不存在的原因改产品。
每次都是拿单个观察往外推：没有对照组，也没先看程序自己的日志——而日志五分钟
就能证明那个房间在本机 12 场全 0 段、第一场早于我们发出的第一个请求。

- **第一步永远是零请求**：`python3 tools/diagnose_room.py @房间 --history-only`，
  按主播聚合会话日志。用户说「之前能用」时，指的是哪个房间，日志说了算
- **同分钟配对**：目标房间和一个当时能用的房间打**同一个接口**（工具默认这么做，
  共 4 次请求）。对照也坏才是本机问题；只有目标坏才谈房间。网页报错和接口返回
  不是同一层，不能拿一个推断另一个
- **4003110 是通用拒绝码**，返回体里没有原因。代码、注释、界面文案、给用户的解释里
  一律只写「接口不给流地址」+ 能做的事；「年龄」「限流」「封禁」这类词不许出现，
  `tests/test_diagnose_room.py` 钉着这条
- 弹幕通 ≠ 流地址能拿：两条路不重叠（评论 WebSocket 不经过 `room/info`）
- 对同一房间的请求要省，绝不开浏览器。改口时要说清楚哪句错了、为什么
